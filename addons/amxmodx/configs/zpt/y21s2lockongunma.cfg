zp_lockongunma_dmg 1.0 // Damage
zp_lockongunma_recoil 0.01 // Recoil
zp_lockongunma_clip 44 // Clip
zp_lockongunma_spd 0.91 // Fire speed
zp_lockongunma_ammo 100 // Ammo
zp_lockongunma_need1 200.0 // Need dmg to shoot 2 grenade's
zp_lockongunma_need2 400.0 // Need dmg to shoot 4 grenade's
zp_lockongunma_need3 600.0 // Need dmg to shoot 6 grenade's
zp_lockongunma_need4 800.0 // Need dmg to shoot 8 grenade's
zp_lockongunma_need5 1000.0 // Need dmg to shoot 10 grenade's
zp_lockongunma_mfspeed 1250 // Grenade fly speed
zp_lockongunma_mcheckdist 2000.0 // Grenade check distance (To victim follow)
zp_lockongunma_mfollowspd 500.0 // Grenade follow speed (To victim follow)
zp_lockongunma_mdistexp 100.0 // Grenade explode distance
zp_lockongunma_mdmgexp 250.0 // Grenade explode damage

zp_lockongunma_surv 0 // Give for survivor [0 = No | 1 = Yes]
zp_lockongunma_extra_cost 100 // Price in extra item

// native zp_get_y21s2lockongunma( iPlayer )